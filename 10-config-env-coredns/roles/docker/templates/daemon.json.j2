{
  "bip": "{{ dockerNetwork }}",
  "registry-mirrors": [
    "http://{{mirrorUrl}}:{{mirrorPort}}",
    "http://{{mirrorUrl}}"
  ],
  "hosts": ["tcp://127.0.0.1:2375","unix:///var/run/docker.sock"]
}
